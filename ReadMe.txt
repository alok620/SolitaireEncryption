This project encrypts or decrypts a message based on a given "deck" (a file with 28 unique numbers).

A deck may be given as a command line argument or typed in as input during runtime.

Choice of whether to encrypt or decrypt is a command line argument.